import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  navigationContainer: {
    backgroundColor: '#121212',
  },
  menuIcon: {
    fontSize: 18,
    marginRight: 12,
  },
});
