import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  navigationContainer: {
    backgroundColor: '#1A1A1A',
  },
  menuIcon: {
    fontSize: 18,
    marginRight: 12,
  },
});
