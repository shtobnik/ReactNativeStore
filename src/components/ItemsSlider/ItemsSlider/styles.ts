import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  wrapper: {
    marginBottom: 20,
  },

  heading: {
    fontSize: 15,
    fontWeight: '700',
    marginBottom: 20,
    color: '#fff',
    paddingHorizontal: 10,
  },
});
