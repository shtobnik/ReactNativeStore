import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  wrapper: {
    marginTop: 10,
  },
  pickup: {
    fontSize: 15,
    color: '#fff',
  },
  free: {
    color: '#13A100',
    fontWeight: '600',
  },
  timer: {
    marginTop: 4,
    fontSize: 14,
    color: '#fff',
  },
  bold: {
    fontWeight: '700',
  },
});
