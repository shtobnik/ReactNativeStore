import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  itemWrapper: {
    alignItems: 'center',
    width: 75,
  },

  iconWrapper: {
    width: 48,
    height: 48,
    backgroundColor: '#333',
    borderRadius: 24,
    alignItems: 'center',
    justifyContent: 'center',
    marginBottom: 8,
  },

  label: {
    color: '#C5C5C5',
    fontSize: 12,
  },
});
