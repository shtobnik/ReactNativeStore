// src/screens/FavoritesScreen/index.tsx
import React from 'react';
import { View, Text } from 'react-native';
import type { FavoritesScreenProps } from '../../navigation/types';

export const FavoritesScreen = (_props: FavoritesScreenProps) => {
  return (
    <View>
      <Text>Favorites</Text>
    </View>
  );
};
